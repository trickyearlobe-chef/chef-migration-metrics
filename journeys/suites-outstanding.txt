# Journeys with no suite yet — the ratchet's grandfathered list.
#
# Not a status claim: nothing here is trusted. `make journey-ratchet` recomputes
# the real set on every run and fails if this file disagrees in either
# direction, so it cannot rot — a journey that gains a suite fails the build
# until it is removed from here, and it can never be added back.
#
# The number only goes down. Removing a line is the point.
journeys/estate-progress.md
journeys/human-verdict.md
journeys/service-collection.md
