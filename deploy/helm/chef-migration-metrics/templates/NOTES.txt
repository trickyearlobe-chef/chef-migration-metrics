Thank you for installing {{ .Chart.Name }}!

Your release is named: {{ .Release.Name }}

To check the status of your deployment:
  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "chef-migration-metrics.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

To access the web dashboard:
{{- if .Values.ingress.enabled }}
  Visit: http{{ if .Values.ingress.tls }}s{{ end }}://{{ (index .Values.ingress.hosts 0).host }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "chef-migration-metrics.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  kubectl get --namespace {{ .Release.Namespace }} svc {{ include "chef-migration-metrics.fullname" . }} -w
{{- else }}
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "chef-migration-metrics.fullname" . }} 8080:{{ .Values.service.port }}
  Visit: http://localhost:8080
{{- end }}

CookStyle & Test Kitchen:
{{- if .Values.chefWorkstation.enabled }}
  Chef Workstation init container is ENABLED ({{ .Values.chefWorkstation.image.repository }}:{{ .Values.chefWorkstation.image.tag }}).
  The following tools will be available: {{ join ", " .Values.chefWorkstation.binaries }}
{{- else }}
  Chef Workstation init container is DISABLED.
  CookStyle scanning and Test Kitchen testing will not be available.
  To enable, set chefWorkstation.enabled=true:
    helm upgrade {{ .Release.Name }} {{ .Chart.Name }} --set chefWorkstation.enabled=true
{{- end }}
